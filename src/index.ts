function numMultiply(a:number, b:number):number {
    return a * b;
}

function nameGreeting(nome:string) :string {
    return 'Olá, ' + nome;
}

console.log(numMultiply(5, 5));
console.log(nameGreeting('João'))